import React from 'react';

import './style.css' //importando o estilo css do header

                                                        //criando um componente
function Header(){//criando o JSX. para exporta-lo ao container pai
    return(
        <header>Trabalhando com componentes</header>
    );
}
//exportando o header
export default Header;